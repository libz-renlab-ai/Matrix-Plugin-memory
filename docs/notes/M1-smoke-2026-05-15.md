# M1 Smoke Results — 2026-05-15

Generated end of M1 inline execution. Status of each Task 24-26 smoke item.

## Task 24 — Full test run

```
npm test   (vitest)
```

**Result**: ✅ **90 / 90 passed** across 17 test files (9 unit + 8 integration).

| File | Tests |
|---|---|
| test/unit/paths.test.cjs | 5 |
| test/unit/schema.test.cjs | 6 |
| test/unit/db.test.cjs | 4 |
| test/unit/events.test.cjs | 4 |
| test/unit/rules.test.cjs | 5 |
| test/unit/confidence.test.cjs | 15 |
| test/unit/redos.test.cjs | 7 |
| test/unit/match.test.cjs | 8 |
| test/unit/analyze.test.cjs | 7 |
| test/unit/extract.test.cjs | 7 |
| test/unit/log.test.cjs | 2 |
| test/integration/sessionstart.test.cjs | 4 (incl. decay + GC) |
| test/integration/userprompt.test.cjs | 2 |
| test/integration/pretooluse.test.cjs | 5 (all four tiers + Edit) |
| test/integration/posttool.test.cjs | 2 |
| test/integration/stop-pipeline.test.cjs | 2 (end-to-end with fake claude) |
| test/integration/cli.test.cjs | 5 |

Plus syntax check: `node --check` over all 13 `.cjs` files in `hooks/`, `hooks/lib/`, `bin/` — all parse.

## Task 25 — Manual `claude --debug` smoke

**Status**: 🟡 **Pending manual run** (cannot be automated in this execution context).

Run from the worktree root:

```
cd plugins/teamagent-memory
npm install --omit=dev          # ensure better-sqlite3 prebuilt is present at runtime
cd ../..
claude --plugin-dir plugins/teamagent-memory --debug
```

In the session, type `please run \`ls\``. Expected debug output:

- `SessionStart` hook fires (creates `~/.teamagent/global.db`, `events.db`, and `<cwd>/.teamagent/knowledge.db`)
- `UserPromptSubmit` hook fires (no rules → no `additionalContext`)
- `PreToolUse` hook fires for Bash → `pretooluse_pass` (no rules)
- `PostToolUse` fires → `posttool_ok`

Then confirm via:

```
sqlite3 ~/.teamagent/events.db "SELECT kind, ts FROM events ORDER BY id DESC LIMIT 5"
```

Should show rows for `session_start`, `prompt_match` (hit_count: 0), `pretooluse_pass`, `posttool_ok`.

## Task 26 — End-to-end correction → rule

**Status**: 🟡 **Pending manual run** (interactive).

In a real Claude Code session (debug on):

```
> please run: npm install moment
(Claude proposes and runs the command)
> don't use moment, use dayjs instead
> /quit
```

After Stop hook completes (≤ 30s), verify:

```
sqlite3 ~/.teamagent/events.db "SELECT kind FROM events WHERE kind LIKE 'stop_%' ORDER BY id DESC LIMIT 10"
# expect: stop_extract, stop_analyze, rule_created (in reverse time order)

sqlite3 ~/.teamagent/global.db "SELECT id, wrong, correct, wilson_lower FROM rules"
# expect: one row, wrong contains 'moment', correct contains 'dayjs', wilson_lower ≈ 0.5-0.6
```

In a NEW session, retry `npm install moment` and confirm PreToolUse returns
`permissionDecision: "ask"` (suggest tier — experimental newly captured rule
has wilson ≈ 0.5, so score = 1.0 × 0.5 = 0.5 falls in suggest band 0.45–0.65).

## Known limitations carried into M2/M3

| Gap | Plan |
|---|---|
| No semantic matching — only fast-path | M2 |
| No override classification — PostToolUse is record-only | M3 |
| `compile` stage is stub (no AGENTS.md propagation) | M5 |
| `match_regex` synthesized by `claude -p` may need iteration | rely on lint + monitoring |
| Stop pipeline serial extract — 5 candidates × ≤ 30s = up to 25s budget | M2 may parallelize |

## What still requires manual confirmation

- [ ] Task 25 — `claude --debug` shows 5 hooks discoverable; DBs created.
- [ ] Task 26 — A real "don't use X, use Y" capture produces a rule.

Run those locally and append PASS/FAIL below.

## End-to-end demo result

- Task 25 manual smoke: TBD by maintainer
- Task 26 end-to-end demo: TBD by maintainer
